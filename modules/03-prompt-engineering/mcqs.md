# Module 03 — MCQ Quiz

**Instructions:** Choose the best answer.

---

**Q1.** Zero-shot prompting means:

- A) Giving the model zero examples before asking the question ✅
- B) Setting the temperature to zero
- C) Using no system prompt
- D) Asking with zero context

---

**Q2.** Few-shot prompting is most useful when:

- A) The task is very common in LLM training data
- B) You need the model to follow a specific format or domain-specific style ✅
- C) You want the cheapest API calls
- D) You're using a local model

---

**Q3.** In Chain-of-Thought prompting, you ask the model to:

- A) Generate multiple answers and pick the best one
- B) Search the internet for information
- C) Show its reasoning step by step before giving an answer ✅
- D) Use few-shot examples in a chain

---

**Q4.** What is prompt injection?

- A) Adding extra context to improve accuracy
- B) An attack where user input overrides system prompt instructions ✅
- C) A technique to inject variables into prompt templates
- D) A method to reduce token usage

---

**Q5.** To make an LLM reliably return JSON, you should:

- A) Use temperature = 0 only
- B) Ask in a system prompt and specify the exact JSON schema ✅
- C) Use a special JSON API endpoint
- D) It's not possible — LLMs can't return JSON

---

**Q6.** Role prompting works best when:

- A) You want random creative outputs
- B) You need the model to adopt specific expertise, tone, or constraints ✅
- C) You want to reduce the context window usage
- D) You're testing hallucination

---

**Q7.** Which is the most effective defense against prompt injection?

- A) Using a more expensive model
- B) Wrapping user input in explicit delimiters and validating the output ✅
- C) Setting temperature to 0
- D) Using only system prompts

---

**Q8.** A system prompt is:

- A) A prompt sent after the user's message
- B) A special prompt that sets the model's role and constraints for the entire conversation ✅
- C) A prompt used to summarize the conversation
- D) A built-in prompt provided by OpenAI

---

**Q9.** You want to generate a SQL query from natural language. Which technique would work best?

- A) Zero-shot with no context
- B) Few-shot with 3 examples of "question → SQL" pairs ✅
- C) Chain-of-thought about SQL theory
- D) Role prompting as "a user"

---

**Q10.** When building a prompt template in Python using `string.Template`, you use `$variable` to:

- A) Define a constant
- B) Mark a placeholder that will be substituted with a real value ✅
- C) Import a module
- D) Define an environment variable

---

**Q11.** Chain-of-thought prompting significantly improves LLM performance on:

- A) Simple factual lookup tasks
- B) Translation tasks
- C) Multi-step reasoning and math problems ✅
- D) Classification tasks

---

**Score:** ___/11
