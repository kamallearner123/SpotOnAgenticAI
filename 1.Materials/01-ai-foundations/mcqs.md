# Module 01 — MCQ Quiz

**Instructions:** Choose the best answer. Submit at the end of Session 1.

---

**Q1.** Which of the following best describes an LLM?

- A) A database that stores facts
- B) A rule-based expert system
- C) A model that predicts the next token based on patterns learned from data
- D) A search engine with AI capabilities

---

**Q2.** What is a "token" in the context of LLMs?

- A) A password used to authenticate with AI APIs
- B) A chunk of text (word or sub-word) that the model processes
- C) A unit of compute used for billing
- D) A type of neural network layer

---

**Q3.** The context window of an LLM refers to:

- A) The screen resolution of the GPU used for training
- B) The maximum number of tokens the model can process at once
- C) The time window during which the model was trained
- D) The number of layers in the neural network

---

**Q4.** What is "hallucination" in the context of LLMs?

- A) When the model responds too slowly
- B) When the model generates fluent but factually incorrect information
- C) When the model refuses to answer a question
- D) When the model repeats the same answer multiple times

---

**Q5.** Which of these is NOT a valid approach to fix LLM hallucination?

- A) Retrieval Augmented Generation (RAG)
- B) Grounding responses in verified data
- C) Increasing the model's temperature
- D) Providing relevant context in the prompt

---

**Q6.** What does "temperature" control in an LLM?

- A) The speed of the GPU during inference
- B) The randomness/creativity of the model's output
- C) The maximum number of tokens in the output
- D) The cost per API call

---

**Q7.** Embeddings are:

- A) Compressed versions of images
- B) Numerical vector representations of text meaning
- C) The weights of the transformer's attention layers
- D) The vocabulary list of a tokenizer

---

**Q8.** What distinguishes Generative AI from classical Machine Learning?

- A) GenAI uses more training data
- B) GenAI creates new content (text, images, audio) rather than classifying existing data
- C) GenAI doesn't require a GPU
- D) GenAI is always more accurate

---

**Q9.** The "transformer" architecture's key innovation is:

- A) Processing text character by character
- B) Using recurrent connections for memory
- C) Self-attention — allowing the model to weigh the importance of different parts of the input
- D) Using decision trees for language tasks

---

**Q10.** If GPT-4 has a 128K token context window and 1 token ≈ 0.75 words, approximately how many words can it process at once?

- A) 12,800 words
- B) 64,000 words
- C) 96,000 words
- D) 128,000 words

---

**Q11.** Which statement about LLMs is TRUE?

- A) LLMs always give the same output for the same input
- B) LLMs have access to the internet by default
- C) LLMs are stateless by default — they have no memory between calls
- D) LLMs can only process English text

---

**Q12.** What was the key innovation that led to ChatGPT's success over earlier language models?

- A) A larger dataset
- B) Reinforcement Learning from Human Feedback (RLHF)
- C) A faster GPU
- D) More transformer layers

---

**Score:** ___/12

---

## Answer Key

| Question | Correct Answer |
| :---: | :---: |
| **Q1** | C |
| **Q2** | B |
| **Q3** | B |
| **Q4** | B |
| **Q5** | C |
| **Q6** | B |
| **Q7** | B |
| **Q8** | B |
| **Q9** | C |
| **Q10** | C |
| **Q11** | C |
| **Q12** | B |
